import {
  hopAroma
} from 'hopunion-aromas/helpers/hop-aroma';

module('HopAromaHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = hopAroma(42);
  ok(result);
});
